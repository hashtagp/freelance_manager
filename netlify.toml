[build]
  command = "npm install --legacy-peer-deps && npm install --save-dev typescript @types/react @types/node && npx prisma generate && npm run build"
  publish = ".next"
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "18"
  NPM_FLAGS = "--legacy-peer-deps"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[context.production.environment]
  NODE_VERSION = "18"

[context.deploy-preview]
  command = "npm install --legacy-peer-deps && npm install --save-dev typescript @types/react @types/node && npm run build"

[context.branch-deploy]
  command = "npm install --legacy-peer-deps && npm install --save-dev typescript @types/react @types/node && npm run build"

# Optional redirects
[[redirects]]
  from = "/old-route"
  to = "/new-route"
  status = 301

[[redirects]]
  from = "/blog/*"
  to = "/blog/:splat"
  status = 200
